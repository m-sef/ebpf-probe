# __ebpf-probe__

This is a shared object library which provides an interface for:
* Intercepting and logging information about received packets via the Extended Berkeley Packet Filter with the Express Data Path hook
* Reading basic hardware counters via perf